#include "main.h"
/**
 * add - print the result of the addition of two integers
 * @a: the first number
 * @b: the second number
 * Return: the addition of both
 */
int add(int a, int b)
{
	return (a + b);
}
